#!/bin/bash

###
# Set up a new, fresh RPi install for wmirror mode
###

# Update apt-get
sudo apt-get update

# Install required packages
sudo apt-get install python3 chromium

# Fetch the latest version of the software from github

# Configure watchdog

# Configure keep alive script

# Configure kiosk mode

# Configure ntp

# Configure logging

# Configure wifi 

# Configure sd card for RO mode (must be done last!)

# Restart!